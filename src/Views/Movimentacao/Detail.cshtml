@using Raiz.ViewModels;
@using Raiz.Enums;

@model MovimentacaoRegister

<h1>Movimentação @Model.Movimentacao.MovimentacaoId</h1>

@if (!string.IsNullOrEmpty(ViewBag.Mensagem))
{
    <div class="alert alert-danger">
        @ViewBag.Mensagem
    </div>
}

<div class="row mb-3">

    <div class="col-2">
        <label>Id</label>
        <input value="@Model.Movimentacao.MovimentacaoId" class="form-control" readonly />
    </div>

    <div class="col-2">
        <label>Documento</label>
        <input value="@Model.Movimentacao.Documento" class="form-control" readonly />
    </div>

    <div class="col-2">
        <label>Data</label>
        <input value="@Model.Movimentacao.DataMovimentacao.ToShortDateString()" class="form-control" readonly />
    </div>

    <div class="col-2">
        <label>Tipo</label>
        <input value="@Enum.GetName(typeof(TipoMovimentacao), Model.Movimentacao.TipoId)" class="form-control" readonly />
    </div>

    <div class="col-2">
        <label>Status</label>
        <input value="@Enum.GetName(typeof(Status), Model.Movimentacao.StatusId)" class="form-control" readonly />
    </div>

    <div class="col-2 pt-4">
        <a asp-action="Index" class="btn btn-secondary">Voltar</a>
    </div>

</div>

<hr />

<table class="table mt-3">
    <thead>
        <tr>
            <th>Produto</th>
            <th>Quantidade</th>
            <th>Preço</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Movimentacao.Itens != null && Model.Movimentacao.Itens.Any())
        {
            foreach (var item in Model.Movimentacao.Itens)
            {
                <tr>
                    <td>@item.Produto?.Nome</td>
                    <td>@item.Quantidade</td>
                    <td>@item.Preco.ToString("C")</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">Nenhum item encontrado</td>
            </tr>
        }
    </tbody>
</table>